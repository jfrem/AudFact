diff --git a/app/Controllers/AuditController.php b/app/Controllers/AuditController.php
index 27a30f6..f0797fc 100644
--- a/app/Controllers/AuditController.php
+++ b/app/Controllers/AuditController.php
@@ -333,6 +333,14 @@ class AuditController extends Controller
             ];
         }
 
+        $accumulatedDurationMs = (int) ($state['accumulated_duration_ms'] ?? 0);
+        $avgDurationMs = (int) ($state['avg_duration_ms'] ?? 0);
+        
+        $throughput = 0.0;
+        if ($accumulatedDurationMs > 0 && ($done + $failed) > 0) {
+            $throughput = round(($done + $failed) / ($accumulatedDurationMs / 1000), 2);
+        }
+
         return [
             'job_id'     => (string) ($state['job_id'] ?? ''),
             'status'     => (string) ($state['status'] ?? BatchJobStore::JOB_STATUS_PENDING),
@@ -340,6 +348,9 @@ class AuditController extends Controller
             'done'       => $done,
             'failed'     => $failed,
             'pending'    => $pending,
+            'avg_duration_ms' => $avgDurationMs,
+            'accumulated_duration_ms' => $accumulatedDurationMs,
+            'throughput_per_sec' => $throughput,
             'created_at' => (string) ($state['created_at'] ?? ''),
             'updated_at' => (string) ($state['updated_at'] ?? ''),
             'audits'     => $audits,
diff --git a/app/Models/AuditStatusModel.php b/app/Models/AuditStatusModel.php
index c96fbfe..ee39f7a 100644
--- a/app/Models/AuditStatusModel.php
+++ b/app/Models/AuditStatusModel.php
@@ -363,7 +363,8 @@ class AuditStatusModel extends Model
                     :DocumentosProcesados AS [DocumentosProcesados],
                     :DocumentoFallido AS [DocumentoFallido],
                     :DuracionProcesamientoMs AS [DuracionProcesamientoMs],
-                    :FacNitSec AS [FacNitSec]
+                    :FacNitSec AS [FacNitSec],
+                    :JobId AS [JobId]
                 ) AS source
                 ON target.[FacSec] = source.[FacSec]
                 WHEN MATCHED THEN
@@ -378,17 +379,19 @@ class AuditStatusModel extends Model
                         target.[DocumentosProcesados] = source.[DocumentosProcesados],
                         target.[DocumentoFallido] = source.[DocumentoFallido],
                         target.[DuracionProcesamientoMs] = source.[DuracionProcesamientoMs],
-                        target.[FacNitSec] = source.[FacNitSec]
+                        target.[FacNitSec] = source.[FacNitSec],
+                        target.[JobId] = source.[JobId]
                 WHEN NOT MATCHED THEN
                     INSERT ([FacSec], [FacNro], [EstAud], [EstadoDetallado],
                             [RequiereRevisionHumana], [Severidad], [Hallazgos],
                             [DetalleError], [DocumentosProcesados], [DocumentoFallido],
-                            [DuracionProcesamientoMs], [FacNitSec])
+                            [DuracionProcesamientoMs], [FacNitSec], [JobId])
                     VALUES (source.[FacSec], source.[FacNro], source.[EstAud],
                             source.[EstadoDetallado], source.[RequiereRevisionHumana],
                             source.[Severidad], source.[Hallazgos], source.[DetalleError],
                             source.[DocumentosProcesados], source.[DocumentoFallido],
-                            source.[DuracionProcesamientoMs], source.[FacNitSec]);";
+                            source.[DuracionProcesamientoMs], source.[FacNitSec],
+                            source.[JobId]);";
 
         $stmt = $writeDb->prepare($sql);
 
@@ -406,6 +409,9 @@ class AuditStatusModel extends Model
         $stmt->bindParam(':DuracionProcesamientoMs', $data['DuracionProcesamientoMs'], PDO::PARAM_INT);
         $stmt->bindParam(':FacNitSec', $data['FacNitSec'], PDO::PARAM_STR);
 
+        $jobId = $data['JobId'] ?? null;
+        $stmt->bindParam(':JobId', $jobId, $jobId === null ? PDO::PARAM_NULL : PDO::PARAM_STR);
+
         $stmt->execute();
 
         Logger::info("AuditStatus: upsert ejecutado", [
diff --git a/app/Services/Audit/Pipeline/AuditAggregationWorker.php b/app/Services/Audit/Pipeline/AuditAggregationWorker.php
index 756bf35..5e9b50d 100644
--- a/app/Services/Audit/Pipeline/AuditAggregationWorker.php
+++ b/app/Services/Audit/Pipeline/AuditAggregationWorker.php
@@ -96,7 +96,8 @@ final class AuditAggregationWorker extends AuditEventConsumer
         }
 
         if ($event->jobId !== null) {
-            $this->jobStore->markAuditCompletedInJob($event->jobId, $event->auditId, $aggregate['final_status']);
+            $durationMs = (int) ($aggregate['audit_result_data']['DuracionProcesamientoMs'] ?? 0);
+            $this->jobStore->markAuditCompletedInJob($event->jobId, $event->auditId, $aggregate['final_status'], $durationMs);
         }
 
         $this->publisher->publish(AuditEvent::create(
diff --git a/app/Services/Audit/Pipeline/AuditStateStore.php b/app/Services/Audit/Pipeline/AuditStateStore.php
index 31a8a05..e1fde74 100644
--- a/app/Services/Audit/Pipeline/AuditStateStore.php
+++ b/app/Services/Audit/Pipeline/AuditStateStore.php
@@ -107,6 +107,21 @@ class AuditStateStore
         ]);
     }
 
+    /**
+     * Marca el inicio del procesamiento activo de una auditoría.
+     *
+     * Se invoca cuando el DocumentAuditOrchestrator consume el evento audit_created.
+     * Permite diferenciar el tiempo de cola (created_at → started_at) del tiempo
+     * de procesamiento activo (started_at → completed_at).
+     */
+    public function markAuditStarted(string $auditId): bool
+    {
+        return $this->patchAudit($auditId, [
+            'started_at' => self::nowUtc(),
+            'status'     => self::AUDIT_STATUS_PROCESSING,
+        ]);
+    }
+
     public function registerDocument(string $auditId, string $documentId, array $documentState): bool
     {
         return $this->runScript(
diff --git a/app/Services/Audit/Pipeline/AuditTimingSummarizer.php b/app/Services/Audit/Pipeline/AuditTimingSummarizer.php
index dd1f86d..0b88055 100644
--- a/app/Services/Audit/Pipeline/AuditTimingSummarizer.php
+++ b/app/Services/Audit/Pipeline/AuditTimingSummarizer.php
@@ -46,31 +46,105 @@ final class AuditTimingSummarizer
             'extraction'     => self::summarizeTimings($samples['extractions']),
             'normalization'  => self::summarizeTimings($samples['normalizations']),
             'policy'         => self::summarizeTimings($samples['policies']),
+            // Métricas de latencia de 3 niveles
+            'processing_duration_ms' => self::resolveDurationMs($audit),
+            'queue_wait_ms'          => self::resolveQueueWaitMs($audit),
+            'total_elapsed_ms'       => self::resolveTotalElapsedMs($audit),
         ];
     }
 
     /**
-     * Calcula la duración total de la auditoría en milisegundos, restando el tiempo
-     * dedicado a llamadas a Gemini desde el tiempo transcurrido entre creación y
-     * finalización.
+     * Calcula la duración del procesamiento activo en milisegundos.
+     *
+     * Prioriza started_at (momento en que el Orquestador consume el evento)
+     * sobre created_at (momento de encolamiento). Si started_at no existe,
+     * hace fallback a created_at para compatibilidad con auditorías en curso.
+     *
      * @param  array<string,mixed> $audit
      */
     public static function resolveDurationMs(array $audit): int
+    {
+        $startKey = self::resolveStartKey($audit);
+        return self::diffToNowMs($audit[$startKey] ?? null);
+    }
+
+    /**
+     * Tiempo de espera en cola: started_at - created_at.
+     *
+     * Retorna 0 si started_at no está disponible (auditoría aún en cola
+     * o single con latencia despreciable).
+     *
+     * @param  array<string,mixed> $audit
+     */
+    public static function resolveQueueWaitMs(array $audit): int
     {
         $createdAt = $audit['created_at'] ?? null;
-        if (!is_string($createdAt) || trim($createdAt) === '') {
+        $startedAt = $audit['started_at'] ?? null;
+
+        if (!is_string($createdAt) || !is_string($startedAt)) {
+            return 0;
+        }
+
+        return self::diffBetweenMs($createdAt, $startedAt);
+    }
+
+    /**
+     * Tiempo E2E total: now() - created_at (siempre incluye cola).
+     *
+     * @param  array<string,mixed> $audit
+     */
+    public static function resolveTotalElapsedMs(array $audit): int
+    {
+        return self::diffToNowMs($audit['created_at'] ?? null);
+    }
+
+    /**
+     * Elige started_at (procesamiento activo) con fallback a created_at.
+     */
+    private static function resolveStartKey(array $audit): string
+    {
+        return isset($audit['started_at'])
+            && is_string($audit['started_at'])
+            && trim($audit['started_at']) !== ''
+            ? 'started_at'
+            : 'created_at';
+    }
+
+    /**
+     * Calcula la diferencia en ms entre un timestamp UTC y now().
+     */
+    private static function diffToNowMs(?string $timestamp): int
+    {
+        if (!is_string($timestamp) || trim($timestamp) === '') {
             return 0;
         }
 
         try {
-            $created = new \DateTimeImmutable($createdAt, new \DateTimeZone('UTC'));
-            $now     = new \DateTimeImmutable('now', new \DateTimeZone('UTC'));
+            $from = new \DateTimeImmutable($timestamp, new \DateTimeZone('UTC'));
+            $now  = new \DateTimeImmutable('now', new \DateTimeZone('UTC'));
+        } catch (\Throwable) {
+            return 0;
+        }
+
+        $diffUs = ((int) $now->format('U') - (int) $from->format('U')) * 1_000_000
+            + ((int) $now->format('u') - (int) $from->format('u'));
+        return max(0, (int) round($diffUs / 1000));
+    }
+
+    /**
+     * Calcula la diferencia en ms entre dos timestamps UTC.
+     */
+    private static function diffBetweenMs(string $from, string $to): int
+    {
+        try {
+            $start = new \DateTimeImmutable($from, new \DateTimeZone('UTC'));
+            $end   = new \DateTimeImmutable($to, new \DateTimeZone('UTC'));
         } catch (\Throwable) {
             return 0;
         }
 
-        $diffUs = ((int) $now->format('U') - (int) $created->format('U')) * 1_000_000
-            + ((int) $now->format('u') - (int) $created->format('u'));
+        $diffUs = ((int) $end->format('U') - (int) $start->format('U')) * 1_000_000
+            + ((int) $end->format('u') - (int) $start->format('u'));
         return max(0, (int) round($diffUs / 1000));
     }
 
diff --git a/app/Services/Audit/Pipeline/BatchJobStore.php b/app/Services/Audit/Pipeline/BatchJobStore.php
index 7d32988..f6aa1ca 100644
--- a/app/Services/Audit/Pipeline/BatchJobStore.php
+++ b/app/Services/Audit/Pipeline/BatchJobStore.php
@@ -94,12 +94,16 @@ class BatchJobStore
         );
     }
 
-    public function markAuditCompletedInJob(string $jobId, string $auditId, string $auditStatus): bool
-    {
+    public function markAuditCompletedInJob(
+        string $jobId,
+        string $auditId,
+        string $auditStatus,
+        int $processingDurationMs = 0
+    ): bool {
         return $this->runScript(
             self::MARK_AUDIT_COMPLETED_IN_JOB_LUA,
             [self::jobKey($jobId)],
-            [$auditId, $auditStatus, gmdate('Y-m-d\TH:i:s\Z'), self::JOB_TTL_SECONDS],
+            [$auditId, $auditStatus, gmdate('Y-m-d\TH:i:s\Z'), self::JOB_TTL_SECONDS, $processingDurationMs],
             'No se pudo actualizar el progreso del job en Redis',
             ['job_id' => $jobId, 'audit_id' => $auditId]
         );
@@ -181,6 +185,7 @@ local auditId = ARGV[1]
 local auditStatus = ARGV[2]
 local now = ARGV[3]
 local ttl = tonumber(ARGV[4])
+local processingDurationMs = tonumber(ARGV[5]) or 0
 
 if type(job['audits']) ~= 'table' or type(job['audits'][auditId]) ~= 'table' then
     return 0
@@ -191,6 +196,7 @@ local previousStatus = tostring(auditState['status'] or '')
 
 auditState['status'] = auditStatus
 auditState['completed_at'] = now
+auditState['duration_ms'] = processingDurationMs
 job['audits'][auditId] = auditState
 
 if previousStatus ~= 'completed' and previousStatus ~= 'manual_review'
@@ -215,6 +221,16 @@ else
     job['status'] = 'processing'
 end
 
+-- Acumulación de tiempo de procesamiento
+if previousStatus ~= 'completed' and previousStatus ~= 'manual_review'
+and previousStatus ~= 'error' and previousStatus ~= 'failed' then
+    job['accumulated_duration_ms'] = (tonumber(job['accumulated_duration_ms']) or 0) + processingDurationMs
+end
+
+if processed > 0 then
+    job['avg_duration_ms'] = math.floor((tonumber(job['accumulated_duration_ms']) or 0) / processed)
+end
+
 job['updated_at'] = now
 redis.call('SET', KEYS[1], cjson.encode(job), 'EX', ttl)
 return 1
diff --git a/app/Services/Audit/Pipeline/DocumentAuditOrchestrator.php b/app/Services/Audit/Pipeline/DocumentAuditOrchestrator.php
index aa6d43a..ebb52fd 100644
--- a/app/Services/Audit/Pipeline/DocumentAuditOrchestrator.php
+++ b/app/Services/Audit/Pipeline/DocumentAuditOrchestrator.php
@@ -52,6 +52,9 @@ final class DocumentAuditOrchestrator extends AuditEventConsumer
             return;
         }
 
+        // Marcar inicio de procesamiento activo — excluye tiempo de espera en cola
+        $this->stateStore->markAuditStarted($event->auditId);
+
         $disDetNro = $this->assertAuditCreated($event);
         $context = $this->buildAuditContext($disDetNro);
         $this->assertIdentityContract($event, $disDetNro, $context);
diff --git a/app/Services/Audit/Pipeline/RulesEvaluationWorker.php b/app/Services/Audit/Pipeline/RulesEvaluationWorker.php
index 29d1e71..eaad9d7 100644
--- a/app/Services/Audit/Pipeline/RulesEvaluationWorker.php
+++ b/app/Services/Audit/Pipeline/RulesEvaluationWorker.php
@@ -331,6 +331,7 @@ final class RulesEvaluationWorker extends AuditEventConsumer
             'DocumentoFallido' => $failedDocument,
             'DuracionProcesamientoMs' => AuditTimingSummarizer::resolveDurationMs($audit),
             'FacNitSec' => (string) ($audit['fac_nit_sec'] ?? ''),
+            'JobId' => ($audit['job_id'] ?? null) !== null ? (string) $audit['job_id'] : null,
         ];
     }
 
diff --git a/frontend/components/audit/audit-timings-panel.tsx b/frontend/components/audit/audit-timings-panel.tsx
index 53f214c..73c3c23 100644
--- a/frontend/components/audit/audit-timings-panel.tsx
+++ b/frontend/components/audit/audit-timings-panel.tsx
@@ -61,9 +61,21 @@ export function AuditTimingsPanel({
     <div className={cn("space-y-5", className)}>
       <div className="flex flex-wrap items-center gap-x-6 gap-y-3 border-b border-white/10 pb-4">
         <InlineMetric
-          icon={<Timer className="h-4 w-4" />}
-          label="Duración total"
-          value={formatMaybeDuration(totalDurationMs)}
+          icon={<Timer className="h-4 w-4 text-emerald-400" />}
+          label="Duración Activa (IA)"
+          value={formatMaybeDuration(timings.processing_duration_ms ?? totalDurationMs)}
+        />
+        {(timings.queue_wait_ms ?? 0) > 0 && (
+          <InlineMetric
+            icon={<Timer className="h-4 w-4 text-amber-400" />}
+            label="Espera en Cola"
+            value={formatMaybeDuration(timings.queue_wait_ms)}
+          />
+        )}
+        <InlineMetric
+          icon={<Timer className="h-4 w-4 text-slate-400" />}
+          label="Duración Total"
+          value={formatMaybeDuration(timings.total_elapsed_ms ?? totalDurationMs)}
         />
         <InlineMetric
           icon={<Gauge className="h-4 w-4" />}
diff --git a/frontend/components/jobs/job-detail-client.tsx b/frontend/components/jobs/job-detail-client.tsx
index c1dd2a0..9d96dd8 100644
--- a/frontend/components/jobs/job-detail-client.tsx
+++ b/frontend/components/jobs/job-detail-client.tsx
@@ -7,7 +7,7 @@ import { useQuery } from "@tanstack/react-query";
 import { CheckCircle2, ExternalLink, XCircle, AlertTriangle } from "lucide-react";
 
 import { auditJobQuery } from "@/lib/query/audit";
-import { formatNumber } from "@/lib/formatters";
+import { formatNumber, formatDurationMs } from "@/lib/formatters";
 import { JobStatusBadge } from "@/components/jobs/job-status-badge";
 import { SectionCard } from "@/components/shared/section-card";
 import { Alert, AlertDescription } from "@/components/ui/alert";
@@ -100,6 +100,17 @@ export function JobDetailClient({ jobId }: { jobId: string }) {
         </div>
       </SectionCard>
 
+      {/* Rendimiento del Lote */}
+      {data?.performance && (
+        <SectionCard title="Rendimiento del Lote">
+          <div className="grid gap-4 md:grid-cols-3">
+            <Metric label="Throughput" value={`${formatNumber(data.performance.throughputPerSec)} docs/s`} />
+            <Metric label="Promedio Activo" value={formatDurationMs(data.performance.avgDurationMs)} />
+            <Metric label="Activo Acumulado" value={formatDurationMs(data.performance.accumulatedDurationMs)} />
+          </div>
+        </SectionCard>
+      )}
+
       {/* Error si existe */}
       {data?.error && (
         <SectionCard title="Error del job">
diff --git a/frontend/lib/schemas/domain.ts b/frontend/lib/schemas/domain.ts
index ef02107..b49051e 100644
--- a/frontend/lib/schemas/domain.ts
+++ b/frontend/lib/schemas/domain.ts
@@ -186,6 +186,9 @@ export const AuditPhaseTimingsSchema = z
     extraction: AuditTimingSummarySchema.nullish(),
     normalization: AuditTimingSummarySchema.nullish(),
     policy: AuditTimingSummarySchema.nullish(),
+    processing_duration_ms: z.number().int().nonnegative().nullish(),
+    queue_wait_ms: z.number().int().nonnegative().nullish(),
+    total_elapsed_ms: z.number().int().nonnegative().nullish(),
   })
   .passthrough();
 
@@ -219,6 +222,9 @@ export const AuditJobSchema = z.object({
   done: z.number().int().nonnegative().optional().default(0),
   failed: z.number().int().nonnegative().optional().default(0),
   pending: z.number().int().nonnegative().optional().default(0),
+  avg_duration_ms: z.number().int().nonnegative().optional().default(0),
+  accumulated_duration_ms: z.number().int().nonnegative().optional().default(0),
+  throughput_per_sec: z.number().nonnegative().optional().default(0),
   created_at: z.string().nullable().optional(),
   updated_at: z.string().nullable().optional(),
 }).passthrough().transform((val) => {
@@ -248,6 +254,11 @@ export const AuditJobSchema = z.object({
     },
     error: null,
     statusUrl: `/audit/jobs/${val.job_id}`,
+    performance: {
+      avgDurationMs: val.avg_duration_ms || 0,
+      accumulatedDurationMs: val.accumulated_duration_ms || 0,
+      throughputPerSec: val.throughput_per_sec || 0,
+    },
   };
 });
 
